const Fonts = {
  "font-roboto": "Default",
  "font-roboto-serif": "Serif",
  "font-roboto-mono": "Mono",
};

const FontSizes = ["font-size-small"];

const PageSizes = ["page-full-width"];

export { FontSizes, PageSizes, Fonts };
